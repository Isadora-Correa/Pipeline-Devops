const test=require('node:test');const assert=require('node:assert/strict');const{readFileSync}=require('node:fs');const html=readFileSync('index.html','utf8');test('apresenta UX Design',()=>assert.match(html,/UX Design/));test('contém Double Diamond',()=>['Descobrir','Definir','Desenvolver','Entregar'].forEach(x=>assert.match(html,new RegExp(x))));test('possui navegação',()=>['#conceito','#processo','#principios'].forEach(x=>assert.match(html,new RegExp(`href="${x}"`))));
